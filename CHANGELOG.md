# 更新日志

本项目遵循语义化版本（pre-release 阶段为 `0.0.x`）。`@version` 单一来源在 `src/meta.js`。

## [0.0.8] - 未发布

Phase 3：正确性修复 + 失败可见性 + 结构收敛（无用户可见的规则行为变更，需重新冒烟）。

### 正确性
- **屏蔽后留下空洞、卡片不重排**：`cellOf` 用 `closest('div.feed-card, div.bili-feed-card, …')` 一把梭定位网格格子，而 `closest` 返回的是**最近的祖先**、不是「优先级最高的选择器」。首页真实结构是 `.container(display:grid) > .feed-card > .bili-feed-card > .bili-video-card`，网格项是 `.feed-card`，旧写法却停在内层 `.bili-feed-card`——隐藏它之后 `.feed-card` 仍占着一个网格单元，于是留下空白且后面的卡不补位。改为按 `CELL_CONTAINERS` 的顺序（由外到内）逐个 `closest`。
- **XHR 实例复用污染**：同一个 `XMLHttpRequest` 对象被复用（`open()` 第二次调用）时，上一次请求缓存的 hook 匹配结果与惰性 getter 不会重置，导致 A 接口的过滤规则被套用到 B 接口的响应上。现在 `open()` 入口无条件重置这些状态。
- **shadow DOM 内的卡片不随规则更新**：规则变更后的重扫只清主文档的已处理标记，shadow root 里的卡片保持旧判定（改了规则也不重判）。卡片扫描与标记清理现在共用同一套「主文档 + 所有存活 shadow root」的根集合。
- **不可信配置可造出单字规则**：导入的 JSON 若把 `keywords` 写成字符串而非数组，`for..of "原神"` 会按字符编译出「原」「神」两条单字规则，足以屏蔽整个首页。新增 `sanitizeConfigInput`（按 `DEFAULT_CONFIG` 形状清洗导入内容）与消费侧兜底 `ruleLines`（规则数组的唯一入口，非字符串元素一律丢弃）。
- **上一条的两个漏网点**：`dualTags` 是唯一没走 `ruleLines` 的规则维度——它不在 `buildMatchers` 里编译，而是每张卡现从 `CONFIG.block.dualTags` 上 `for..of` + `.length`，于是既绕过了防线（字段是字符串时 `.length` 非零，会白白去拉每张卡的标签接口），也让 `ApiDim.active` 的类型被迫写成 `() => boolean | number`。现在与其它维度一样在 `buildMatchers` 里编译成 `M.dualTags`（预拆分量并小写，热路径不再每张卡跑一遍 `split/map/filter`；原行原样保留，`维度:规则` 原因串不受影响），`active` 收回 `() => boolean`。屏蔽记录面板里的 `CONFIG.block.uids.map(String)` 同理改走 `ruleLines`——字段被写坏时 `.map` 会直接抛，把整个记录面板打空。新增用例覆盖全部 8 个可定位维度被写成字符串的情形。
- **未渲染的评论被永久跳过**：`processComment` 在读 `__data` **之前**就写下 `host.__bfbCmtV = ruleVersion`，而评论宿主常常先入 DOM、数据后到。于是紧接着那句 `if (!c.uname && !c.message) return; // 还没渲染出数据，等下一轮` 永远等不到下一轮——本规则版本内这条评论再也不会被评估，代码与自己的注释相互矛盾。改为数据到齐后才打版本号。
- **`isLive` 的含义随开关漂移**：它原先只在 `hideAd || hideLiveCard` 时才计算，但 `processCard` 拿它来区分「无标题的直播卡」与「尚未渲染的骨架卡」——两个开关都关时，每一张无标题直播卡都会被判成骨架、每轮扫描重抠一次。字段含义不该随配置变化，现在直播识别（两次 `querySelector` + 一次本卡 `textContent`，很轻）恒定计算，只有真正昂贵的广告角标全卡遍历仍跟着 `hideAd` 走；`DetectFlags` 随之只剩 `detectAd` 一个。
- **`分区` 会匹配到推荐理由**：拦截层归一 JSON 时，`partition` 在 `tname`/`typename` 缺失后兜底取 `rcmd_reason.content`，而那是「已关注 / 高播放」这类**推荐角标**，不是分区——`分区:` 规则与 `part:` 关键词会因此莫名其妙地命中。JSON 这一路本就拿得到权威分区字段，没有理由降级去用一个语义不同的字段，兜底删除。
- **多开标签页会丢规则**：`CONFIG` 是内存单例，`saveConfig()` 把它**整份**覆盖写回存储。于是同时开着两个 B 站标签页时，A 页加的规则会被 B 页随后的任何一次存盘冲掉——B 写的是它自己那份加规则之前的旧快照，而存盘会被拦截计数这类后台改动悄悄触发，用户根本不知道自己「保存」了什么。现象是「这脚本有时候记不住规则」，且全程无报错；开得越久的标签页覆盖掉的东西越多。现在经 `GM_addValueChangeListener` 监听存储变更（借鉴 `nanatuo/bilibili-blocker`）：任一标签页写入后，其余标签页 300ms 内整份采纳并重建规则、重扫页面、刷新面板，把丢失窗口从「两个标签页的整个生命周期」缩到「一次存盘防抖」。
  采纳走 `deepMerge` 就地并入而非 `Object.assign` 换对象——面板输入框在渲染时绑定的是 `CONFIG.block` 这类**对象引用**，换掉之后用户再改设置就写进了脱钩的旧对象，界面有反应、配置不变、也不报错。正在面板里打字时跳过重渲（配置已生效，只是显示滞后），否则会清掉没提交完的输入。老版本脚本管理器没有这个 API 时降级为无同步，不影响其它功能。

- **「增大首页推荐每批加载数量」会把请求改成必然失败的形状**：该功能的 preFn 把首页推荐 URL 里的 `ps=` 改大，而它匹配的正则同时覆盖了 `wbi/`（已签名）那一路。B 站的 WBI 签名是把**全部** query 参数按 key 排序后连同 `mixin_key` 一起 MD5 得出 `w_rid`——签名覆盖每一个参数，签完再动任何一个，服务端校验必然对不上、直接返回 `-403 校验失败`，那次推荐请求就白发了。首页推荐早已迁到 wbi 路径，所以这不是理论风险；只因这个开关默认关闭（opt-in）才一直没人踩到。（该结论参考 `tjsky/TabulaBili` 的实测记录与 `SocialSisterYi/bilibili-API-collect` 的 wbi 文档。）
  兜底放在**管线出口**而非那条正则里：`NET.rewriteUrl` 现在只要发现原始 URL 带 `w_rid`，就丢弃本轮全部改写结果、原样发出。按「有没有签名标记」这个确定性特征判定，B 站以后再把别的接口迁去 wbi 也自动安全，不必等下一次踩坑再补一条正则。代价是该功能在已签名接口上不生效——这件事被显式报出来而不是静默吞掉：`health.signedSkipped` 计数，「工具 → 🩺 运行自检」列出，面板开关的说明里也写明。未签名的旧路径上它照常生效。

- **一次网络抖动会让一批视频在整个会话里失去联网维度**：`fetchView` / `fetchTags` / `fetchCard` 过去把结果**连同失败时的 `null`** 一起写进长期缓存，于是 `cache.has(key)` 恒为真——那个 bvid/mid 此后再也不会重试。一次超时、一次断流、一段风控退避，就能毒化一批 key，标签、双标签、充电、UP简介这些维度对它们静默失效。方向虽是安全的那一侧（漏过而非误伤），但它静默且不可恢复，用户只会觉得「标签规则有时候不灵」。
  现在按响应性质分流：`code === 0` 成功→长期缓存；`code !== 0` 且非风控码是服务端的**确定性否定**（稿件不存在/已删除，结论不会变）→长期缓存 `null`；无响应 / 响应不是 JSON / 风控码属**瞬时失败**→不进长期缓存，只压 30 秒冷却后可重试。冷却是必要的，完全不记会让每轮重扫对同一批 key 重发请求，风控期间尤其糟。冷却表的键带命名空间前缀——view 与 tag 都以 bvid 为键，共用一张表会让一次 view 失败连带压住 tag 请求。三个取数函数就此收敛到同一个骨架 `cachedGet`。

- **上一条（多标签页同步）的两个漏网点**：采纳时保住嵌套**对象**的引用还不够。`deepMerge` 在对象层靠递归保住了身份，数组层却是直接赋值——而面板的名单控件闭包持有的正是数组本身（`chipModel(CONFIG.block.keywords)`）。于是别的标签页一改配置，本页所有名单控件就绑到了脱钩的旧数组上：用户回车加一条规则，界面显示「已添加」，存盘里什么都没有。这恰好命中「正在打字时跳过重渲」那条分支，也就是最可能有人正在加规则的时刻。现在 `deepMerge` 的不变量收紧为「只改内容、不换 base 里已存在的对象或数组」。
  数组元素仍会被换成新对象（这层保不住，也不该保）。规则订阅那一行的两个动作正好踩在这上面：启用开关写的是渲染时闭包持有的 `sub` 对象，删除按的是渲染时的下标，而删除还要先过一个确认弹窗——跨越这段异步之后写回旧对象等于写进空气，按旧下标删则可能删掉相邻的另一条订阅。两者改为按 `url` 现查现改。

- **正则测试器会对引擎不收的正则报「命中」**：编译器出于 ReDoS 防护会整条忽略过长或疑似灾难性回溯的 `/正则/`（`(a+)+` 这类），而测试器自己 `new RegExp` 现编现测，于是同一条正则在测试器里「✅ 命中」、抄进名单后一次都不生效，且没有任何提示——测试器存在的意义正是提前告诉你规则会不会生效。现在两处共用同一个判据 `regexRejectReason`（返回可直接展示的拒收理由），测试器改为显式提示「会被规则引擎忽略」。顺带也不再真的去跑它：测试器是 `oninput` 实时执行的，跑一条灾难性回溯正则就是卡死面板。

- **右键 / 悬停在 shadow DOM 里的卡片上没有反应**：`onContextMenu` 与 `onCardHover` 都用 `e.target.closest(VIDEO_CARD_SELECTOR)` 定位卡片，而事件穿过影子边界时 `e.target` 会被**重定向**到宿主元素，从它出发的 `closest` 永远走不进那棵影子树。于是出现了自相矛盾的状态：扫描器早就会遍历 shadow root 隐藏卡片，同一张卡却右键不了、悬停也不出「拉黑 / 不看这个」。同一个文件里的评论分支早就用 `composedPath` 解决过这个问题（还专门写了注释），视频卡这条一直没跟上。现在两处共用 `findCard`，统一走 `composedPath`（它给出的是未重定向的完整路径，对影子树内外一视同仁），`composedPath` 不可用时退回原写法。
  这同时是「兼容 BewlyCat」的第一步——那类把整个界面挂进 shadow root 的界面替换型扩展，卡片天然都在影子树里。但要说清楚：BewlyCat 是**浏览器扩展**，其 content script 跑在 isolated world，它发的请求与我们在页面上下文里 hook 的 `fetch` 不是同一个，因此本脚本的网络拦截层（主力）对它取的数据看不见，在那种环境下只有 DOM 层能工作，属**降级兼容**，不承诺等同效果。
- **存档损坏会静默清空全部设置**：`loadConfig` 解析失败时直接返回默认配置，而 `CONFIG` 一旦成了默认值，随后**任何一次** `saveConfig` 就把那份也许只是被写截断、还能人工抢救的原始内容永久盖掉——存盘会被拦截计数这类后台改动悄悄触发，往往几秒内就发生。用户看到的是「所有设置一夜之间回到出厂」，没有提示，也没有补救余地。现在解析失败时先把原始内容原样另存到 `bfb_config_corrupt_backup`（首次为准，第二次损坏不覆盖第一份），再回落默认值，并在页面就绪后弹一次错误 toast + 把原文打进控制台。

- **shadow DOM 里新增的卡不触发重扫**：`scanner` 的 `MutationObserver` 只 `observe(document)`，而**影子树内部的 DOM 变动不会冒泡到 document 级观察器**。于是 shadow root 里后续新增的卡永远不会触发重扫，只能靠光 DOM 的某次变动偶然捎带一次——表现为「有的拦了有的没拦」「滚一会儿就不拦了」，且不报任何错。现在每个进注册表的 shadow root 都被单独观察一次：`shadow.ts` 收口成单一入口 `addShadowRoot` + 一个「发现新 root」回调，scanner 在回调里挂观察器（注册时会对已收集的 root 补跑——`attachShadow` 钩子先于 `startScanner` 安装，中间创建的 root 否则永远没人观察）。B 站自己的 shadow 组件与界面替换类扩展都受这一条影响。

- **⚠ 规则会被别的标签页的「后台计数」冲掉（P0，本轮最重要的一条）**：`saveConfig()` 是「把内存里整份 CONFIG 覆盖写回」，而 `stats` 每拦到一个视频就 `blockedCount++` 并触发存盘。于是「B 标签页挂在后台刷首页」这件毫无危害的事，会用它那份**加规则之前的旧快照**把你刚在 A 页加的规则整份抹掉。上一版引入的 `GM_addValueChangeListener` 只把丢失窗口从「标签页的整个生命周期」缩到「一次广播延迟」，关不掉竞态——两边在同一个窗口内先后写入，先写的照样没了。用户侧就是「更新完脚本，我原来屏蔽拉黑的 UP / 视频没了」。
  两条一起改：① **写时三方合并**——`saveConfig` 写之前先读回存储里当下的内容，与「本页上次和存储一致时的样子」逐字段比对，只把本页真正改过的部分盖上去；名单按**集合**合并（本页新增的加进去、本页删掉的删出来，其余保留对面的），于是两个标签页各改各的都留得住，「谁写得晚」不再决定结果。键的「消失」靠 base 区分是「对方新增」还是「本页删除」，否则删除永远同步不出去（规则停用表取消最后一条时就会踩到）。② **高频写与低频写不再共用一把锁**——`blockedCount` / `ruleStats` / `uidNames` / `ruleStatsSince` 搬到独立的 `bfb_stats_v1`，后台计数从此完全不碰规则那份存储。内存里它们仍住在 `CONFIG` 上，各模块无需改动；老存档里写在一起的那份照样读得回来。
- **评论折叠展开后收不回去**：占位灰条的点击处理是**单向**的——`ph.remove()` 把灰条整个删掉、`__bfbCmtExpanded` 永久置位，于是展开之后页面上再没有可点的东西，只能刷新页面。而「点开看一眼、确认是垃圾、再收回去」恰恰是这个功能最自然的用法。现在灰条常驻、可来回切换（文案在「已折叠 · 点击展开」与「已展开 · 点击收起」之间切），`__bfbCmtExpanded` 只保留它原本的职责：挡**自动**重扫，不再兼任「永久展开」的开关。顺带把占位条文案改成 `textContent` 写入，不再走 `innerHTML` + 转义。

- **面板「清空名单」既不存盘也不生效**：`model.clear()` 的实现只有 `arr.length = 0`，后面既没有 `saveConfig()` 也没有 `emitRulesChanged()`。两条后果：刷新页面规则**全都回来**（用户只会以为「清空按钮坏了」）；匹配器没重建，`M` 里还编译着那些规则，于是**界面显示空列表、页面照旧屏蔽**。旁边的删单条 / 删除所选 / 删除匹配都走 `removeFromList`（内含存盘 + 发事件），唯独整列清空漏了。现在收进 `rules.clearLists()`，与其它删除路径同源。

### 性能
- **批量删除的写放大**：`removeFromList` 看着是「从数组里删一条」，实际展开是「全量三方合并存盘（含两次全量深拷贝、两次序列化）+ 重建全部匹配器（含全量解析订阅缓存）+ 全页重扫」。而面板的「删除所选 / 删除匹配」是在**循环里**逐条调它——勾 100 条就是这一整套跑 100 遍，几千条名单下是秒级的主线程冻结外加 100 次写存储。讽刺的是 `ARCHITECTURE.md §9` 有一条红线专门写了这件事，写入侧（`doBlacklistMany` / 预置库 / 名单批处理 / 账号黑名单导回）全都老老实实用 `pushUnique` + 末尾一次存盘，唯独删除侧一直没有对称件。新增 `rules.removeEntries()`（按数组分组、`Set` 索引、就地压缩、末尾一次存盘+重扫），三个批量调用点换过去；顺带把 `removeFromList` 里 `arr.map(String).indexOf` 的 O(n²) 收成 O(n)。
- **订阅缓存每次规则变更都全量重新解析**：`collectSubRules()` 挂在 `buildMatchers` 与 `enumerateRules` 上，而它每次都 `loadSubStore()` = `GM_getValue` + `JSON.parse`。订阅的 uids/upNames/bvids 各自上限 5 万条，多订几个源就是数 MB——每次改规则、每次开面板的规则体检都要解析一遍。改为解析结果缓存 + 写入侧失效（并监听别的标签页对订阅键的写入，避免用旧规则）。
- **三方合并里的无效 `JSON.stringify`**：`mergeList` 对每个元素做两次 `JSON.stringify` 比较，而字符串数组里 `keyOf(x)` 就是它自己，同 key 必然同值、比较恒为 false——5 万条 UID 的名单每次存盘白跑 10 万次序列化。字符串直接短路，只有对象元素（订阅）才真比。`threeWayMerge` 的标量分支同理先用 `===` 短路。
- **屏蔽记录面板的二次复杂度**：展开后每行都要判「这个 UID 在不在黑名单里」（`ruleLines(CONFIG.block.uids)` 会复制整个数组再 O(n) 查找）和「这条原因对应哪条规则」（`locateRule` 线性扫描该维度全部规则行，每行 trim 两次 + 跑一次正则）。100 行 × 5 万条名单 = 单次刷新数百万次字符串比较，而这在滚首页时被反复触发。现在每次刷新只建一次 UID `Set`；`locateRule` 内置按 `ruleVersion` 失效的反查索引（所有调用方受益，不只是记录面板）。
- **UP 名解析的请求放大**：解析成功的回调是「`saveConfig()` + 整列 chip 重建」，而重建会把本次渲染的请求配额重置成满额，于是为**还在飞行中**的 UID 又发一轮；`cachedGet` 又只查已完成的缓存、没有 in-flight 去重，这些是真的重复请求。一批解析能放大成数百个请求 + 数十次全量存盘——为「把数字显示成名字」这件事付出的代价过高，且有实际的风控风险。两处都改：接口层加在途请求表（同 key 排队等第一个结果，`evaluateApi` 那边同一 bvid 出现在多张卡上时也一并受益），解析回调改为攒批（一批解析 = 一次存盘 + 一次重渲）。
- **配置备份拖慢每次面板重渲**：备份索引与内容原先存在同一个键里，而面板每次重建都会读一遍索引——等于「为了显示三行文字，`JSON.parse` 几百 KB × 5 份」。改成索引（时间/版本/规则条数）与内容分开存，内容只在用户点「恢复」时按需读；被挤出保留份数的内容键一并删除，不留没人引用的大字符串。
- **评论组件的影子树不再驱动全页卡片重扫**：上一版给每个 shadow root 都挂了卡片观察器，而评论区的懒加载、展开楼中楼、hover 都会持续产生 mutation，每一次都触发一轮全页卡片重扫。评论里没有视频卡，也早有自己的扫描路径，现在按宿主标签名跳过。顺带删掉观察器里那次 `harvestShadowRoots(document)` 全文档扫描——`attachShadow` 已被 hook，新建的 root 一律当场入表，那次全量采集捞不到任何新东西（与 `shadow.ts` 文件头写的「只在新增子树里增量采集」也自相矛盾）。
- **`shadowRoots` 的回收不再被总开关挡住**：失效 root 的清理原先只发生在 `queryAllRoots` 里，而它的入口都有 `if (!CONFIG.enabled) return`——脚本一暂停，detached 的评论组件连同整棵子树就被钉在内存里，每个还挂着观察器。「回收没用的引用」和「用户开没开拦截」毫无关系，改为独立的定时回收。
- **`escapeHtml` 的映射表提到模块级**：原先写在 `replace` 回调里，每匹配一个字符就新建一次对象字面量，而屏蔽记录面板每次刷新要调它几百次。

### 可观测性（失败可见）
- **运行自检 `src/health.ts`**：这类脚本最典型的故障是静默失效——B 站换个接口路径或类名，脚本照常跑、角标照常显示，只是什么都不再拦。现在在拦截层与 DOM 层埋轻量计数器，首屏稳定后（3.5s）在控制台输出告警，面板「工具」页新增「🩺 运行自检」区可随时查看：接口请求数 / 形似推荐流的请求数 / 命中推荐接口数 / 解析出列表数 / 识别卡片数。
  判据特意区分了「本页压根没发过推荐流请求」与「发了但没接住」——B 站首页首屏是 SSR（HTML 直接带 10 张卡），推荐接口要滚动或「换一换」才发，此时给中性说明而非报警，只有出现形似推荐流的请求却一个都没命中时才判定路径变更。

- **拦截管线疑似失效时角标变色**：静默失效是这类脚本的头号故障，而此前唯一的报警渠道是控制台——可会看控制台的人本来就能自己排查，真正需要被告知的人永远看不到。现在 `healthDegraded()` 判定「发过形似推荐流的请求却一个都没接住」「接住了却取不出列表」「一张视频卡都没识别到」这三类**管线断了**的情形时，角标转橙并显示「⚠ 拦截可能已失效」。不弹 toast——误报的代价是骚扰所有人，而变色是零打断的：平时粉的东西今天黄了，足以让人点开自检看一眼。判据比控制台报警更保守：首屏稳定之前一律不报（那时计数天然都是 0），WBI 签名放弃改写也不算（那是某个开关不生效，不是脚本坏了）。

### 可解释性 / 误伤自愈
- **拦截原因带上命中的具体规则**：过去关键词维度只报一个光秃秃的「关键词」，UP 简介只报「UP简介」——用户拿到一条误杀记录，得去自己上百条规则里猜是哪条干的。现在原因串统一为 `维度:规则`（`关键词:原神`、`UP简介:恰饭`、`标签:/游戏.*/`）。标签/分区维度也从「报视频的标签值」改为「报命中的规则」，因为要改的是规则不是视频。
  实现上没有动热路径：判定仍走 `compileLines` 合并成的那条正则，只在**确定命中之后**才 `whichHit` 逐条回查一次（每次拦截一次，而非每张卡一次）。
- **记录行「✂删规则」**：从一条屏蔽记录一键定位并删除肇事规则。旁边原有的「✅放行」只赦免这一个 UP，规则还会继续误伤别人；关键词误伤的病灶通常是规则写得太宽，得治规则。规则来自订阅（不在用户名单里）时不给删除按钮，改标一个「订阅」角标说明去哪儿停用——不谎报能删。已拉黑的行不给此按钮，避免与「↩撤销拉黑」语义混淆（删本地 UID 规则并不会把人移出账号黑名单）。
- **规则体检（工具页）**：规则是一年年攒下来的，攒的过程没有任何反馈——加了就再也不知道它有没有用。于是两类问题一直烂在名单里：一条过宽的两字词天天误伤，一条打错字的规则一次都没生效。新增「🩹 规则体检」区：列出**最常命中**的规则（过宽的嫌疑）与**观察满 7 天仍零命中**的规则（写错/过时的嫌疑），后者就地给「✂删」。
  为此新增持久化的规则级计数 `ruleStats`（会话日志只有 300 条、刷新即清，判不出「从来没用过」这种长期结论）。三条自我约束，宁可少报也不冤枉正在生效的规则：观察不足 7 天一律不报；「精确过滤」关闭时联网维度（标签/双标签/UP简介）根本不会被求值，零命中归到「未启用」而非死规则；订阅里的规则删不掉，不报。计数随规则删除自动清理，不让存档随「加了又删」无界膨胀。该数据属个人使用记录，导出/导入均不携带。
- **UP主 维度改报规则而非视频里的 UP 名**：两者仅大小写不同时（规则 `SomeUP`、视频 `someup`），原因串报视频值会让反查按精确比对失败——「✂删规则」按钮静默消失，规则体检还会把这条天天在拦的规则误报成「从未命中」。
- **分类汇总改按维度聚合**：原因串带上具体规则后，按完整原因分组会让分类栏碎成几十项（`关键词:原神×3`、`关键词:恰饭×2`…）。分类只看维度，具体规则在每条记录里看。

### 体验
- **名单支持搜索（超过 8 条时出现）**：名单是一年年攒起来的，攒到几十上百条后「这词我是不是加过」「把那条打错字的删掉」全靠在 chip 堆里用眼睛找。搜索语法与规则本身一致——普通词=包含（忽略大小写），`/.../`=正则，不另发明第二套写法。UID 条目按数字和解析出的 UP 名都搜得到（人记得住的是名字不是数字），组合标签按存储写法 `a+b` 和显示写法 `a & b` 都搜得到。
  搜索生效时，**批量操作一律只作用于筛出来的条目**：「全选」变「全选匹配」，「清空」变「删除匹配(N)」并在确认框里写明保留多少条——按搜索结果操作是这功能的全部意义，也是它唯一能酿成大祸的地方。改搜索词会清空勾选，以维持「勾选集 ⊆ 屏幕上看得见的」这条不变式（否则搜 A 勾三条、再搜 B 勾两条，「删除所选(5)」会连屏幕外那三条一起删）。添加成功后自动撤掉筛选，否则新加的条目多半不匹配当前搜索词，看起来像「加完就不见了」。
- **首屏不再「先闪一下再消失」**：`MutationObserver` 原先在 `DOMContentLoaded` 才装（`observe(document.body)`），而 B 站首页首屏是 SSR——卡片由解析器一张张吐出来、拦截层（改 JSON）完全够不着，等观察器装好时它们早已绘制在屏幕上，于是该屏蔽的视频会先显示、再消失。现在扫描调度独立成 `scanner.ts`，在 `document-start` 就观察 `document` 本身（不依赖 `<html>`/`<body>` 是否已存在，避免「元素还没生成 → 观察器没装上 → DOM 层整层静默失效」）。
  分两个阶段合批：首屏解析中用 **rAF**（每帧一次、绘制前，这正是浏览器为「赶在绘制前改」提供的原语），首屏之后回到 250ms 节流（滚动加载晚几十毫秒无人察觉）。
  刻意**不**采用「先用 CSS 蒙住整个 feed、判完再放出来」的常见做法：那要赌脚本不出错，一旦本层抛异常用户就对着永久空白的首页。现方案只隐藏**肯定命中**的卡，脚本挂掉就什么都不隐藏，退化成「不过滤」而非「看不见内容」——失败方向必须是安全的那一侧。

- **播放页可以右键屏蔽当前视频的 UP**：过去只有视频卡能右键，而播放页上正在看的那个视频的 UP 主信息区不属于任何一张卡，右键毫无反应——偏偏那才是最想屏蔽的时刻（看到一半发现是营销号）。现在播放页右键 UP 名 / 头像 / 联合投稿的合作成员，都能弹出 屏蔽 UP / 拉黑 UP / 加白名单 / 屏蔽此视频。
  判据用「指向 `space.bilibili.com` 的链接」而不是类名：UP 名、头像、合作成员在 B 站历次版式里都是这样的链接，类名却改过好几轮。范围收得很紧以免抢掉浏览器原生右键菜单——只在播放页生效，顶栏里的 space 链接（你自己的头像）和评论组件里的用户名都排除在外（后者尤其重要：评论区自己那条右键分支只在「评论区过滤」开着时才走，关着时若不排除就会把评论者加进**视频**黑名单）。UP 信息区的选择器故意不进 `VIDEO_CARD_SELECTORS`——那是扫描器的输入，混进去会让 UP 信息区被当成卡片判定甚至隐藏。

- **BewlyCat 等界面替换类扩展下的降级兼容**：它把整个界面挂进 `mode:'open'` 的 shadow root（`src/contentScripts/index.ts`），配合上面的 `composedPath` 与 shadow root 观察两项修复，右键菜单、悬停浮层、DOM 层屏蔽都能工作了。选择器上补三条它的类名：卡片标题 `.video-card-title`、UP 名 `.channel-name`、网格项 `.video-card-container`（它的卡片**内层**类名恰好是 `.video-card`，已被原有选择器命中；不补网格项的话隐藏内层会留空洞）。作者链接仍是 `//space.bilibili.com/{mid}`，UID 与 BV 号照常抠得到。这三个类名在原生 B 站不存在，无回归风险。
  实测 DOM 后补齐了余下三个维度：时长 `.video-card-cover-stats__item--duration`、播放量 `.cover-stat-view .video-card-cover-stats__value`、点赞数 `.cover-stat-like .video-card-cover-stats__value`。播放/弹幕/点赞/时长四项**共用** `.video-card-cover-stats__value` 这一个类名，所以必须带上父级的 `cover-stat-*` 限定——只写子类名会按文档序取到第一个，而谁排在前面取决于用户的显示设置，「按播放量屏蔽」会时灵时不灵且不报错。点赞数在此之前 DOM 这一路**完全没有来源**（只有接口 JSON 有 `stat.like`），于是「营销号低赞率」维度在纯 DOM 场景恒不生效，顺手接上。
  ⚠ **明确说明能力边界，不承诺等同效果**：BewlyCat 是**浏览器扩展**，其 content script 跑在 isolated world，发出的请求与本脚本在页面上下文 hook 的 `fetch` 不是同一个，因此**网络拦截层（主力）对它的数据完全失明**，在它的界面下只有 DOM 层能工作，拦截量会明显低于原生界面。

- **规则可以「停用」，而不是只能删**：规则只有增删两态时，面对一条疑似过宽的规则，用户唯一能做的动作是删掉——而删掉不可逆，于是大多数人选择放着不管，坏规则在名单里越攒越多。人真正想做的是「先关两天看看有没有变化」。现在每个名单 chip 上都有 `⏸`：停用后灰显带删除线、**不参与编译**，但仍留在名单里，随时 `▶` 打开。规则体检里「⏸停用」排在「✂删」前面（更保守的选项放在更顺手的位置），被停用的规则单列一行、**不会**被报成「从未命中」的死规则——它没命中是你自己按下去的。停用状态不参与导出/导入（属个人使用状态，与 `ruleStats` 同理），但会跨标签页同步。
  实现上停用只作用于**编译**：`enumerateRules` 故意不过滤它们，否则 `pruneRuleStats` 会把停用规则的历史命中数当垃圾清掉，重新启用后它看起来像条崭新的规则、随即又被报成死规则。
- **删规则可撤销**：拉黑早就有撤销红线，删规则却一直是不可逆的——而误删规则的概率比误拉黑高得多。现在 chip 的 `✕`、屏蔽记录的「✂删规则」、规则体检的「✂删」都会弹出带「撤销」的 toast，撤销把规则插回**原来的位置**而不是追加到末尾（名单顺序是用户自己攒出来的，撤销的语义是「刚才那下不算」）。
- **动态流（t.bilibili.com）接入拦截层**：此前它是唯一一个完全靠 DOM 兜底的主要页面——只能等卡片画出来再隐藏，且抠不到 UID 这类权威字段，图文/纯文字动态更是根本判不了。现在接进 `FEED_HOOKS`，与首页同源同判：命中的动态从数据里就被删掉，从头不出现。
  动态的响应结构与推荐流没有一处重合（字段全埋在 `modules` 下），所以给 `FeedHook` 开了个可选的 `norm`——让每个形状怪异的接口带自己的归一函数，好过把 `normFeedItem` 变成谁都不敢改的大杂烩。转发动态会连同**被转发的原动态正文**一起参与判定（否则「转发引战内容」拦不到）。只删 `data.items`，不动 `offset`/`has_more`：分页游标由 B 站维护，改它会打乱后续加载。

- **配置自动备份 + 规则骤降熔断（数据安全兜底）**：三方合并解决的是「不再被覆盖」，属于**防止事故**；但规则是用户攒了几个月的东西，事故真发生了（我们没想到的 bug、别的脚本、手滑）也得有救。两条兜底都与「具体是哪个 bug」无关：① 检测到脚本 `@version` 变了，在本次运行写任何东西**之前**，先把存储里那份原样存一份——升级是最危险的时刻，旧标签页还跑着旧代码而用户此刻正在密集操作；② 一次写入若让规则总数掉光、或骤降 5 条以上，把写入前的内容也存一份并显式告警。滚动保留最近 5 份，面板「工具 → 🗂 配置备份」可查看（时间 / 版本 / 当时多少条规则）并一键回滚，恢复前还会先把当前状态再备一份。
  刻意**不**拒绝写入：清空列表、删除匹配、恢复默认都是正当操作，拦下来只会变成「我删不掉规则」这种新 bug。备份 + 告警既不改变任何行为，又让每一次清空都可回滚。备份键独立于 `STORE_KEY`——备份要能在规则那份被写坏之后仍然读得出来，就不能和它住在一起。
- **「从账号黑名单导回本地」**：「拉黑」实际写两处——B 站账号黑名单（服务端，**权威**）与本地 `block.uids`（镜像，只为让 DOM 层也能拦）。镜像是会丢的，而账号那份还好端端的；此前唯一的办法是对着官方黑名单页一个个手抄。现在「工具 → 名单批量处理」多一个按钮，分页拉取 `relation/blacks` 把名单重建出来，顺带把 UP 名填进 `uidNames`（光有一串数字，用户在面板里根本认不出自己拉黑过谁）。读取失败时明确区分「拿不到」与「你的黑名单是空的」——后者会让人以为账号那边也没了。

- **大名单（几千条）下的显式截断**：从账号黑名单导回后名单可达几千条，三处会出问题，且都是**静默**的：① 导入的页数上限写成了 40 页（2000 人），超出部分被悄悄丢掉、界面上还是报「成功」——上限提到 400 页，并把结果如实拆成「账号里共 N / 本次读到 M / 本地新增 K」，撞上限时明说「可能未读完」；② 名单 chip 全量建 DOM 会让面板卡住几秒——改为最多渲染 300 个，并写明「还有 N 条未显示，**批量操作仍作用于全部 N 条**」（悄悄只显示一部分、批量却动了全部，才是真会出事的组合）；③ 缺 UP 名的 UID chip 会**逐个**发请求解析名称，打开一次面板就是几百个请求换一轮风控——限到每次渲染 20 个，其余照常显示 UID（搜索按数字也找得到）。规则体检的死规则列表同样限到 50 条并说明剩余数量：UID 类规则观察期一到大多会被判成「从未命中」，那只是还没轮到被推荐，不是写错了。
  数据层一条都不许少算——新增的规模用例锁住「3000 条 UID 匹配照常命中、存盘读回不丢、`countRules` 与规则体检如实计数」，截断只允许发生在**显示**这一层。

- **账号黑名单导入接入风控熔断**：上一条新写的导入只把响应码喂给了 `riskGuard`，却从不检查它——旁边的批量拉黑一直是检查的，这条路漏了。后果是第 5 页触发风控后，第 6…60 页照旧一头撞上去，把退避窗口撑得更长；而且第一个非 0 码就整批中止，前面读到的几十页全丢。现在：翻页前先看熔断状态，退避中就等；风控码视作**可重试**（等退避结束重试同一页，最多 4 次，反复撞墙则带着已读到的部分收工），只有未登录这类确定性失败才中止；翻页节奏从固定 300ms 改为 400ms + 300ms 抖动（3000 人约 35 秒——只读接口，慢一点换不触发风控划算得多）；退避期间界面如实显示「触发风控，已暂停约 Ns 后自动继续」，而不是卡在一个不动的数字上。

### 结构
- **扫描调度拆出 `scanner.ts`**：`dom.ts` 只管扫描**什么**（判定逻辑），`scanner.ts` 只管**何时**（时机策略）。策略部分 `createScanScheduler` 用依赖注入接 raf/timeout，得以脱离真实 DOM 单测（node 环境没有 MutationObserver/rAF）。
- **面板拆分**：原 `ui/panel.ts`（973 行、单个近千行的 `renderPanel`）拆为 `ui/panel/`——`index.ts` 只管 Tab 骨架与分区注册表，14 个分区各自成文件（`sections/*`），只依赖 `ctx.ts` 这一个接口。加一个面板分区从「在千行函数里找位置」变成「写个文件 + 往数组加一行」。
- **全部 14 个面板分区移除 `@ts-nocheck`**，恢复类型检查。这不是逐处加断言硬压下来的，四处结构性改动解决了绝大多数报错：
  - 新增取值器 `q<T>(root, sel)`：面板 HTML 是本模块自己用模板串渲染的**静态**结构，取不到某个 id 只可能是「模板与代码不同步」的编程错误，于是直接抛（错误边界会记到控制台），而不是每处写 `if (!el) return` 把 bug 藏起来——约 150 处 `querySelector(...)!` 断言由此消失，且模板漂移从静默半残变成响亮失败。
  - `CONFIG.block[field]` 原本类型是 `string[] | number`（含 `minDuration` 等阈值字段），取值处只能各自 cast。改为在 `match/engine.ts` 定义 `RuleListField` 联合类型，规则行字段与阈值字段在类型层面分开。
  - `bindControl` / `chipModel` / `blacklistUp` 的尾参在实践中本就是可选的（`@ts-nocheck` 下无人察觉），改在**定义处**声明默认值，而不是在每个调用点补参。
  - 每个分区显式标注 `: PanelSection`，注册表与实现之间的契约由编译器守。
  开启检查当场逮到一个真 bug：`sections/rule-health.ts` 里「✂删死规则」按钮把 `confirmModal` 当成对象参数调用（实际签名是 `(message, opts) => Promise<boolean>`），点下去只会抛异常、永远删不掉——这段代码上一次提交时是「看起来对的」。（另 7 个模块见下条，至此 `src/` 全量类型化。）
- **`@ts-nocheck` 清零**：剩下的 7 个模块（`dom.ts` / `blacklist.ts` / `comments.ts` / `main.ts` / `ui/field.ts` / `ui/menu.ts` / `ui/panel/index.ts`）也已摘掉。同样不是逐处加断言压下来的，每一类报错都当成「这个契约该写在哪」来解：
  - **`src/gm.ts`（新）**：`GM_xmlhttpRequest` 的类型垫片。`@types/tampermonkey` 的 `Request` 里没有 `withCredentials`，而拉黑与视频详情**必须**带 Cookie；过去在调用点写 `} as any)`，代价是把整个请求对象的检查一并丢掉（`onload` 打错名字、`data` 拼错字段都不报错，而这些是发出去才知道的错）。现在 4 个 GM 调用点恢复受检，且环境里没有 `GM_xmlhttpRequest` 时返回 `false` 让调用方显式降级。
  - **拉黑的跨层契约由 `blacklist.ts` 导出**（`BlockTarget` / `BlockResult` / `BlockProgress` / `BlockController`），`batch-block.ts`、`name-list.ts` 里那几个本地「替身接口」删除。接上真类型当场暴露一处漂移：本地替身把失败码写成 `number | string`，实际是 `number | null`，于是批量拉黑的失败分类会把网络错误显示成 `code null×3`——现在如实显示为「网络错误×3」。
  - **元素上的附加字段集中声明**：卡片的 `_bfbInfo` 收进 `cacheCardInfo/cachedCardInfo`；评论宿主的 `__data` / `__upMid` / `__bfbCmt*` 收进 `CommentHost` 接口，并把 `Element → CommentHost` 的收窄集中到 `asCommentHost()` 一处（右键菜单不再自己拼断言）。顺带把「我们依赖了 B 站哪些字段」写成了一份可查的清单。
  - `COMMENT_TAGS` 的值域改为 `boolean | undefined`（查表用的是任意 `tagName`，「查不到」是常态而非异常），新增 `isCommentTag()` 供三个调用点共用，`comments.ts` 里的 `CMT_TAGS` 转发别名删除。
  - `ui/menu.ts` 的两个浮层按钮由两个可空模块变量改为一个整体（要么都没建、要么都建好，位置/显隐操作不必再各自判空）；事件 target 的收窄改用运行期 `instanceof Element`，而不是散落的 `as Element`。
  - `ui/panel/index.ts` 的 `buildPanel()` 改为返回面板根节点，调用方不必再 `getElementById` 一遍去应付「理论上的 null」——顺带去掉了首次打开时白渲染一遍的重复调用。
  至此 lint 里为未类型化模块兜底的 `no-undef` 与 `UNTYPED_FILES` 名单一并删除，`ban-ts-comment` 对 `ts-nocheck` 的豁免也收回：再想整文件关掉类型检查得先过这条规则，防止「加个 nocheck 先跑起来」重新变成常态。
- **迁移期的 lint 豁免全部收回**：`eslint.config.js` 里那组「等类型化完再收紧」的关停规则（空 `catch`、`arguments`、`x && x()`、`this` 别名、正则字符类）到期兑现，一并把对应的 29 处代码改了而不是把规则继续关着：回调统一写 `cb?.(…)`；5 个空 `catch` 补上「为什么可以吞」的说明（吞异常的理由必须写下来，否则下一个人无法判断该不该改）；`XHR.open` 的 `async/user/password` 从 `arguments[2..4]` 转正为具名可选参数，「原样透传」这件事从此看得见。真正的例外只剩 3 处，改用**定点** `eslint-disable-next-line` + 理由注释（表情正则要的就是按码点拆掉组合表情；两处 `this` 别名分别是给 `defineProperty` 的 getter 和 `closest` 的起点用的），这样同类问题在新代码里仍会被拦下——而不是像整条规则关掉那样对所有人静默放行。全角空格改为只在模板串与注释里允许（中文文案的排版是有意的，代码里的则是误输入）。
- **拉黑与撤销拉黑收敛为一个请求函数**：两者打的是同一个 `relation/modify`，只差一个 `act`，此前却是两份约 95% 相同的代码（取 csrf、拼表单、解析响应、喂熔断器各写了一遍）——改一处忘另一处只是时间问题。现在 `relationModify(uid, act, done)` 只管「发请求 + 归一响应」，本地名单怎么改、弹什么文案、成功怎么算留给两个调用方（这部分两个动作本就不同，硬塞进去只会换成一堆回调参数）。归一结果带一个三态 `outcome`（未登录 / 网络错误 / 拿到响应）：这三种情况 `code` 都可能不是正常业务码，但对用户要说的话完全不同，只看 `code` 分不开——过去正是靠代码路径的位置隐式区分的，合并时若不显式建模就会把「网络错误」说成「账号侧失败（code null）」。
- **`collectSubRules()` 的返回值恢复真类型**：`match/engine.ts` 里两处 `const sub: any = collectSubRules()` 把 `SubRules` 丢掉了，起因是 `dualTags` 不属于订阅可携带的维度、按 `SubDim` 索引会报错。改为一个类型守卫 `isSubDim` + 共享的 `userAndSubLines(dim, sub)`：「这个维度有没有订阅来源」从一次 `any` 变成编译器认得的事实，`buildMatchers` 与 `enumerateRules` 里那两段各写一遍的「用户名单 ∪ 订阅」也合成了一处。
- **选择器登记表 `src/selectors.ts`**：散落在 5 个模块里的 B 站 DOM 选择器字面量集中到一处。B 站改版时只需改这一个文件。
- **配置结构版本 `schemaVersion`**：存档带版本号 + `migrateConfig` 迁移链，为将来重命名字段/改变量纲留出无损升级路径（纯新增字段不需要升版本，`deepMerge` 会补默认值）。
- **`net.ts` 移除 `@ts-nocheck`**：拦截层恢复完整类型检查。

### 性能
- **统计通知合批**：一次响应过滤可能连删几十项，原先每项都同步触发一次 UI 刷新 + 存盘调度；现在攒到本轮同步代码结束后的微任务里只通知一次。
- **日志惰性求值**：热路径（每张卡都会调一次的放行日志）改为传函数，debug 关闭时连消息字符串都不拼。

### 测试 / CI
- **拦截层契约测试**：新增 `tests/fixtures/`（推荐/排行/热门/相关/搜索五个接口的脱敏样本）+ `tests/net.test.ts`（22 例），覆盖 URL 路由、各接口的列表提取契约、过滤与白名单、开关与审查模式、畸形数据容错、健康计数。B 站改结构时纯逻辑测试发现不了，这类 fixture 测试可以。
- **可解释性闭环测试** `tests/explain.test.ts`：逐维度走真实的 `matchRule`/`matchApi`（不手写原因串），核对产出的原因都能被 `locateRule` 反查回名单里的那一行。维度产出与 `REASON_RULE_FIELD` 一旦漂移，「删规则」按钮会静默消失或指向错字段，而这种失效不报任何错。
- **共享 DOM 替身 `tests/helpers/dom.ts`**：`page.ts` / `cardinfo.ts` 的**全部逻辑就是选择器逻辑**，不喂真元素树等于没测，而仓库刻意不引入 jsdom（依赖只保留 esbuild + vitest + eslint + typescript）。于是把原先散在 `page.test.ts` 里的手写替身抽成共享件并补全：支持 `tag/.cls/#id/[attr*=v]` 复合与后代选择器，且严格照规范语义（`closest` 取**最近祖先**、`querySelector` 取**文档序**首个）——这两条正是这类代码最容易踩的坑，替身放宽就测不出来。
  据此新增 `isUnsafeHideTarget`（隐藏护栏：大容器 / 多卡元素判危险，隐错会连带删掉无限滚动的加载哨兵 → 整页空白且加载不出新内容）与 `extractCardInfo`（选择器优先级、UID 三级降级、`deepUid=false` 不做 innerHTML 兜底、广告角标必须精确等于「广告/赞助/推广」而非包含）的用例。
- **评论判定测试** `tests/comments.test.ts`（22 例）：评论过滤有十几条规则、四条白名单，此前一条测试都没有——因为逻辑与 DOM 操作缠在一个模块里，而仓库没有 jsdom。现在把「判定」那一半（`readCmt` / `matchComment`，纯函数）导出单测：白名单优先级（UP 本人 / 置顶 / 自己，含 mid 跨类型判等）、楼中楼前缀与 @提及不参与匹配（否则会被「被 @ 者的昵称」误伤）、等级缺失不误伤、默认头像非会员、召唤 AI、带货、纯 @ / 纯表情，以及 `__data` 缺字段时不抛错的安全默认值。
- **版本号检查**：CI 新增 job——PR 里改了 `src/` 却没升 `src/meta.js` 的 `@version` 时失败（否则已安装脚本的用户收不到更新）。

## [0.0.7] - 未发布

### 体验
- **悬停「不看这个」单条隐藏**：卡片悬停浮层在「拉黑」下方新增「🚫 不看这个」，一键只隐藏当前这条视频（按 BV 号屏蔽，刷新后仍隐藏，带「撤销」提示，可在黑名单 BV 列表移除）。比拉黑整个 UP 更轻量。

## [0.0.6] - 2026-06-24

Phase 2：安全与正确性强化（有行为变更，需重新冒烟）。

### 安全
- **收窄网络权限**：`@connect` 去掉 `*`，仅声明 `api.bilibili.com` 及常见 CDN 域名。自定义域名的订阅/名单 URL 首次访问会由 Tampermonkey 弹一次性授权确认。
- **导入剥离不可移植键**：配置导入侧与导出侧对称剔除 `NON_PORTABLE`（尤其 `subscriptions`），防止他人分享的「规则文件」借导入悄悄塞入会自动联网拉取的订阅 URL。
- **正则 ReDoS 防护**：订阅/导入来源的 `/正则/` 模式体超过 `MAX_REGEX_LEN`（1000）时忽略，避免灾难性回溯卡死页面。

### 正确性
- **联合投稿回调修正**：`blacklistUp` 联合投稿路径改用结果对象 `{added,already,total,failed}`，修复旧版误按 `(n,total)` 取参导致文案与回调恒错。
- **批量拉黑一次性重扫**：批量本地屏蔽不再逐条存盘 + 全页重扫，改为收尾统一一次，消除大批量时的卡顿。
- **feed 逐项容错**：拦截层逐项 `try/catch`，单条畸形数据不再放弃整条响应的其余过滤。

### 安全 / 体验
- **单条拉黑二次确认**：右键菜单与悬停浮层的拉黑执行前需确认（账号写操作不可一键撤销）。
- **批量拉黑可停止**：名单批量新增「停止」按钮，停止时把未处理 + 失败的 UID 回填输入框便于续传；`doBlacklistMany` 返回可取消的控制器。
- **批量进度**：当前页批量在按钮上实时显示进度 / 风控暂停状态。
- **日限提示**：名单数量较多（>200）时提示账号黑名单总量上限与单日大批量易触发风控，建议分批。

### 体验 / 可达性
- **样式化确认弹窗**：所有确认对话框（拉黑、删订阅、恢复默认、清空列表、开启精确过滤等）从原生 `confirm()` 升级为与面板同风格的弹窗，支持 `Esc` 取消 / `Enter` 确认、点击遮罩取消，危险操作默认聚焦「取消」降低误确认，`role=dialog`/`aria-modal` 提升可达性。
- **toast 分级配色**：动作结果提示按结果上色——风控/网络错误=红、未登录/账号侧失败=橙、拉黑成功/已在黑名单=绿，一眼可辨（中性提示保持原样）。
- **拉黑「撤销」**：把不可逆的账号拉黑变为可恢复——单条拉黑成功后弹出带「撤销」按钮的 toast（8 秒），面板「屏蔽记录」中已拉黑的条目常驻「↩撤销」按钮；撤销走 `relation/modify act=6`，账号侧移出黑名单 + 本地恢复，刷新后该 UP 恢复推荐。面板记录里的「⛔拉黑」也补齐了二次确认。
- **暗色模式**：自有 UI 全表面跟随系统 `prefers-color-scheme` 暗色适配（面板/弹窗/toast/右键菜单/输入/按钮/chip/Tab/滚动条），品牌色与语义色保留。
- **键盘可达性**：面板 `Esc` 关闭、打开时焦点移入面板、关闭时归还焦点；关闭 ✕ 支持 `Enter`/`Space`；为按钮/Tab/链接等补 `:focus-visible` 焦点环。
- **对比度**：提示/统计/空态/分组提示等说明文字提到 WCAG AA（≥4.5:1）。

### 性能 / 内存
- **API 缓存容量上限**：`view`/`tag`/`card` 缓存改用带上限的 `capMapSet`（800/1200/800），超限按插入顺序淘汰最旧键，避免长会话内存无界增长（淘汰项下次重取，结果不变）。
- **`uidNames` 软上限**：持久化的 UP 名缓存上限 5000；已存在键照常更新，仅新增键达上限时跳过，避免存档 blob 无界膨胀。
- **观察器（评估后保持现状）**：`MutationObserver` 仍按批全量 `scanAll`，但单卡昂贵判定已被 `PROCESSED` 短路、每批仅一次原生 `querySelectorAll`（开销极小）。增量化重写收益微小却有 shadow/skeleton 卡片覆盖回退风险，故暂不改，待真实 profiling 证据再议。

## [0.0.5] - pre-release

模块化重构基线：将单文件用户脚本拆分为分层 TypeScript 模块（L0 常量/工具 → L5 UI），引入 esbuild 打包、vitest 单测、ESLint。行为与重构前等价。
